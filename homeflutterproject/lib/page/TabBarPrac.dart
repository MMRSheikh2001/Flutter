import 'package:flutter/material.dart';

class Tabbarprac extends StatelessWidget {
  const Tabbarprac({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
    );
  }
}
