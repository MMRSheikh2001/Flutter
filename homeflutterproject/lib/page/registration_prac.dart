import 'package:flutter/material.dart';

class RegistrationPrac extends StatelessWidget {
  const RegistrationPrac({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
